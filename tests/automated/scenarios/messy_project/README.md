# Parallel Data Loader

A project for loading data in parallel.

## Usage

```python
from loader import ParallelLoader
loader = ParallelLoader(num_workers=4)
data = loader.load("path/to/data")
```

## Status

Work in progress.
